on:
  workflow_call:
    inputs:
      PROJECT_ROOT:
        description: 'the root path of the project to build'
        required: true
        type: string

runs:
  using: composite
  build:
    runs-on: ubuntu-24.04
    steps:
    - name: hello
      run: building {{ inputs.PROJECT_ROOT }}